# @package _global_

experiment:
  name: debug_model_smoke

train:
  mode: bootstrap

system:
  seed: 42
  device: cpu
  num_workers: 0

dataloader:
  batch_size: 4

model:
  return_debug_outputs: true
